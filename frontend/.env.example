# URL of Person 4's FastAPI backend. Copy this file to .env and adjust if
# the backend runs somewhere other than localhost:8000.
# If unset/unreachable, the dashboard automatically falls back to mock data.
VITE_API_BASE_URL=http://localhost:8000
